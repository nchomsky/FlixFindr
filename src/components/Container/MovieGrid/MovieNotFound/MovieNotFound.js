import React from 'react';

const MovieNotFound = () => {
    return (
        <div className="noResults">
            Unfortunately, we could not find the movie you were looking for. <span>Please try searching for a different movie.</span> 
        </div>
    );
}

export default MovieNotFound